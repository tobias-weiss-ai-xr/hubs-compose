#!/bin/bash
basedir=$(readlink -f "$(dirname "$0")"/..)

mutagen-compose -f "$basedir"/docker-compose.hubs.yml down --volumes --rmi local &&
rm -rf "$basedir"/services/reticulum/deps &&
"$basedir"/bin/init
